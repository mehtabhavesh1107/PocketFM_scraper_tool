# Pocket FM Frontend

React + Vite dashboard for the local Pocket FM commissioning tool.

Run from the repo root:

```text
npm run dev:frontend
```

The dev server runs at `http://127.0.0.1:5173` and proxies `/api` to the local FastAPI backend at `http://127.0.0.1:8000`.
