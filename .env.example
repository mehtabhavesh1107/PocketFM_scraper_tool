# Local dev and Vercel Services use the same-origin API path.
# Override only if the backend is hosted on a separate origin.
VITE_API_BASE_URL=/api

# Optional but recommended for persistent cloud data on Vercel.
# Without this, Vercel falls back to ephemeral /tmp SQLite storage.
# COMMISSIONING_DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE
